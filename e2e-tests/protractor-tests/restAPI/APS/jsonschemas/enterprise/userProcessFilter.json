{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "userProcessFilter.json",
  "title": "User process filter details",
  "description": "User process filter schema",
  "type": "object",
  "properties": {
    "id": {
      "description": "user process filter id",
      "type": "integer",
      "minimum": 1
    },
    "name": {
      "description":"user process filter name",
      "type": "string",
      "pattern": "^[a-zA-Z0-9]+"
    },
    "appId": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "recent": {
      "type": "boolean"
    },
    "icon": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9]+"
    },
    "filter": {
      "$ref": "processFilter.json"
    }
  },
  "additionalProperties": false
}